name: copy contetns

on:
  push

jobs:
  copy:
    name: copy
    runs-on: ubuntu-latest
    steps:

    - name: Read and assign
      id: output_test
      run: |
        contents = $(cat new.txt)
        echo $contents

    # test to see if done correctly - should echo "value"
    - run: echo ${{steps.output_test.outputs.new_value }}
