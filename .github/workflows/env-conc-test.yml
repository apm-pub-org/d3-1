name: Envs

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  j1:
    runs-on: ubuntu-latest
    concurrency: temp1
    environment: 
      name: temp1
      url: https://google.com/
    steps:
      - name: print
        run: |
          sleep 120 && echo "done"
  j2:
    runs-on: ubuntu-latest
    steps:
      - name: print
        run: |
          sleep 120 && echo "done"
